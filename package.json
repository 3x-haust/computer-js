{
  "name": "computer-js",
  "private": true,
  "version": "0.2.0",
  "description": "Computer.js — call real macOS capabilities from a webpage with user approval",
  "workspaces": ["sdk"],
  "scripts": {
    "build:sdk": "npm --workspace sdk run build",
    "build:native": "cd native && swift build -c release && bash Scripts/make_app.sh && bash Scripts/make_dmg.sh",
    "test:sdk": "cd sdk && COMPUTER_RUNTIME_AUTO_APPROVE=1 node e2e.test.mjs"
  }
}
